# Archived Pi pack workflow

Archived on 2026-08-31 because the Pi pack and SwarmForge workflows are no longer used.

Contents:

- `skills/pi-sixpack/`
- `profiles/pi-subagents/two-pack.json`
- `profiles/pi-subagents/four-pack.json`

These paths are outside `skills/` and `profiles/pi-subagents/`, so Pi does not load them.

To restore an item, move it back to its original path and rerun `python3 scripts/check-docs.py`.
